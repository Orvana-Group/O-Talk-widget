export interface Config {
    botId: number
    clientId: number
    botName: string
    firstMessage: string
    bgColor: string
    primaryColor: string
    isAudioEnabled: boolean
    isFilesEnabled: boolean
}
